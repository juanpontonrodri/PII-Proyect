/**
 * Interface to model the method that will change the luminosity.
 * @author Juan.
 * @version 1.0.
 */
public interface Luminosity{
    /**
     * Method to change the luminosity.
     * @param value The integer to increment the luminosity.
     */
    public abstract void changeLuminosity(int value);
}